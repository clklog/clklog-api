# 审计项全量对照（审核用）

> 总览见 [安全修复报告.md](../安全修复报告.md)  
> 已修复详单：[P0.md](./P0.md) · [P1.md](./P1.md) · [P2.md](./P2.md)

编号与 WorkBuddy《ClkLog 代码审计报告》2026-07-31 对齐。  
每个优先级下拆成两块：**已修复** / **未修复**（含误报、业务暂缓、部分未完）。

| 状态图例 | 含义 |
|----------|------|
| ✅ 已修复 | 代码已改，详单见对应 P0/P1/P2 |
| ⚠ 部分已修 | 核心已消，残余仍列在「未修复」或说明中 |
| ❌ 未修复 | 暂未改代码 |
| ⊘ 误报 | 不成立或与产品设计不符 |
| ◐ 业务暂缓 | 承认问题，改口径/架构成本高，约定暂不动 |

---

## P0

### 已修复

| # | 项 | 状态 | 说明 / 去向 |
|---|----|------|-------------|
| 1 | ~~硬编码 DB / JWT / Redis 弱凭据~~ | ✅ 已修复 | 改为 `${ENV:默认}`；JWT 两端同密钥。详 [P0.md](./P0.md) P0-6 |
| 2 | ~~Actuator / Druid 匿名公开~~ | ✅ 已修复 | 仅 `/actuator/health` 公开。详 P0-7 |
| 3 | ~~CORS `credentials` + `origin=*`~~ | ✅ 已修复 | `*` 时自动关 credentials。详 P0-8 |
| 4 | ~~Cookie 明文存密码~~ | ✅ 已修复 | 只存 username。详 P0-1 |
| 5 | ~~Vuex 硬编码 `password: 'clklog'`~~ | ✅ 已修复 | 已删除。详 P0-2 |
| 6 | ~~崩溃原因 `v-html` XSS~~ | ✅ 已修复 | 改文本插值。详 P0-3 |
| 7 | ~~Token 使用 `innerHTML`~~ | ✅ 已修复 | Vue 数据切换。详 P0-4 |
| 9 | ~~axios 0.18.1 CVE~~ | ✅ 已修复 | 升至 1.7.9。详 P0-5 |

### 未修复

| # | 项 | 状态 | 说明 / 原因 |
|---|----|------|-------------|
| 1b | 仓库内弱密码默认值 / Redis 可空 / MySQL 默认 root | ❌ 未修复 | 兼容本地与现网；生产靠环境变量与运维加固（与 #1 可配置化相关） |
| 8 | 前端硬编码 `roles: ['admin']` | ◐ 业务暂缓 | 社区版无后端 RBAC；产品即管理端 |
| 10 | MySQL 初始化默认账号 admin/clklog | ◐ 业务暂缓 | 业务需要默认账号，密码均为 `clklog` |

---

## P1（审计 14 项；#14 拆为 a/b）

### 已修复

| # | 项 | 状态 | 说明 / 去向 |
|---|----|------|-------------|
| 1 | ~~`sortOrder` SQL 注入~~ | ✅ 已修复 | 仅允许 asc/desc。详 [P1.md](./P1.md) P1-1 |
| 2 | ~~`limit` 超大分页~~ | ✅ 已修复 | `PageLimitUtil` max 500。详 P1-2 |
| 3 | ~~API Key 明文打日志~~ | ✅ 已修复 | 日志脱敏。详 P1-3 |
| 4 | ~~`maskApiKey` 短 Key 越界~~ | ✅ 已修复 | 先判长度。详 P1-4 |
| 9 | ~~`CLKLOG_LOG_DB` 拼进 SQL（库名校验）~~ | ⚠ 部分已修 | 库名校验已做。详 P1-5；其余脚本注入面见下方未修说明 |
| 10 | ~~`InOrderCalcProcessBoss` stop NPE~~ | ✅ 已修复 | 成员 `thread` 赋值。详 P1-6 |
| 12 | ~~生产 `console.log` 敏感调试~~ | ✅ 已修复 | 仅开发环境打印。详 P1-8 |
| 13 | ~~生产环境加载 Mock~~ | ✅ 已修复 | 仅非 production 启用。详 P1-7 |
| 14a | ~~登录 `redirect` 开放重定向~~ | ✅ 已修复 | `sanitizeRedirect`。详 P1-9 |

### 未修复

| # | 项 | 状态 | 说明 / 原因 |
|---|----|------|-------------|
| 5 | API Key 过期应用层未校验 | ❌ 未修复 | 现依赖 Redis TTL；乱加校验可能误杀。位置：`TokenService`（api）等 |
| 6 | `channel_detail_bydate` 缺渠道列 | ⊘ 误报 | 产品以 `lib` 为渠道，表与查询已按 `lib` 聚合 |
| 7 | `log_analysis` ORDER BY 不佳 | ❌ 未修复 | `init.sql`；改排序键需迁表 |
| 8 | 聚合 INSERT 无严格幂等 | ❌ 未修复 | `InitServiceImpl`；现靠 ReplacingMergeTree；强行先删再插易改数 |
| 9（残余） | 其它脚本注入面（若有） | ⚠ 部分已修 | 库名已校验；若审计另指脚本面，新增时记于此 |
| 11 | 用户生命周期仅 4 天窗口 | ◐ 业务暂缓 | `visitor_life_bydate.sql`；改窗口会改流失/沉默指标 |
| 14b | 独立 Token 刷新机制 | ❌ 未修复 | 前端无 refresh；后端已有滑动续期，改动面大 |

---

## P2（审计 15 项）

### 已修复

| # | 项 | 状态 | 说明 / 去向 |
|---|----|------|-------------|
| 1 | ~~`e.printStackTrace()`~~ | ✅ 已修复 | 改 logger，约 16 文件。详 [P2.md](./P2.md) P2-1 |
| 3 | ~~ApiKeyService 空 catch~~ | ✅ 已修复 | `logger.warn`。详 P2-2 |
| 4 | ~~ThreadLocal DateFormat 未 remove~~ | ✅ 已修复 | Filter 请求结束清理。详 P2-3 |
| 9 | ~~趋势 SQL 用 GB2312 读~~ | ✅ 已修复 | 改 UTF-8。详 P2-4 |
| 11 | ~~`formatNumber` 恰 1 亿显示异常~~ | ✅ 已修复 | `>=1亿` 显示「亿」。详 P2-5 |

### 未修复

| # | 项 | 状态 | 说明 / 原因 |
|---|----|------|-------------|
| 2 | 异常吞没 / 误导日志 / HTTP 仍 200 | ⚠ 部分已修 | 部分路径已有业务 code=500；未做大范围接口契约改动 |
| 5 | Spring Boot 2.6.4 已知 CVE | ❌ 未修复 | 大版本升级，回归成本高 |
| 6 | jjwt 0.9.1 CVE | ❌ 未修复 | API 变更，需全面回归登录鉴权 |
| 7 | `visitTime = max-min` 精度不足 | ◐ 业务暂缓 | **改指标计算口径** |
| 8 | `is_first_day` 用 String | ◐ 业务暂缓 | 改表结构 + 全链路字段 |
| 10 | `percentage` / `averageRules` 双实现不一致 | ❌ 未修复 | 统一会改变部分页面数字观感 |
| 12 | 无防暴力破解 / 无密码复杂度 | ❌ 未修复 | 改登录体验与现网账号策略 |
| 13 | CSRF 全局禁用 | ❌ 未修复 | JWT + Bearer 常见；强开需前端配合，易误伤 |
| 14 | `ReportServiceImpl` 超 3800 行 | ❌ 未修复 | 大重构，易引入回归 |
| 15 | `cal_date` 用当前毫秒而非轮次固定 | ◐ 业务暂缓 | 可能改聚合日期归属 |

---

## 状态统计（便于盖章/汇报）

| 优先级 | 已修复 | 部分已修 | 误报 | 业务暂缓 | 未修复 |
|--------|--------|----------|------|----------|--------|
| P0 | 8 | 0 | 0 | 2 | 1（弱默认残余） |
| P1 | 9（含 14a） | 1（#9） | 1（#6） | 1（#11） | 4（#5/#7/#8/#14b） |
| P2 | 5 | 1（#2） | 0 | 3 | 6 |

---

## 维护约定

1. **新发现未修** → 追加到对应优先级的「未修复」表。  
2. **完成修复** → 从「未修复」挪到「已修复」（删除线 + ✅），并写 P0/P1/P2 详单；**编号不丢**。  
3. **误报** → 留在「未修复」表，状态 ⊘。  
4. 详单代码只写在 P0/P1/P2；总报告只汇总链接。
